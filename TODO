For Version 0.3
    Reduce lag produced by html requests 
        -- incl default responses in script
        -- save html page to file for persistent queries on first run (make optional)
        -- reduce number of if|else conditional stmts
    
For Version 1.0
    Port to PyPi
        -- Write setup.py and improve documentation
